name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: Build with Buildozer
        uses: EnisHustla/buildozer-action@v2
        with:
          buildozer_version: master
          command: buildozer android debug
